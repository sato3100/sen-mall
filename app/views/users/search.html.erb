<h2>出品者検索</h2>

<%= form_for :user_search, url: search_users_path, method: :get, local: true do |f| %>
  <div>
    <%= f.label :q, "ビジネス名" %><br>
    <%= f.text_field :q, value: (@keyword || "") %>
    <%= f.submit "検索", class: "btn btn-primary" %>
  </div>
<% end %>

<hr>

<h3>検索結果</h3>
<% if @users.present? %>
  <ul>
    <% @users.each do |seller| %>
      <li>
        <%= link_to "#{seller.business_name}", seller_show_path(seller) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>お探しのアカウントが見つかりませんでした</p>
<% end %>
